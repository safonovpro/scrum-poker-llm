# Session Log - Scrum Poker LLM Deployment

**Date:** 2026-05-30  
**Duration:** ~8 hours  
**AI Assistant:** Koda (NLP-Core-Team)

## Overview

Production deployment of Scrum Poker application. Frontend on GitHub Pages, backend in Docker container on Ubuntu server.

## Deployment Setup

### 1. Backend Docker Configuration
- **File:** `backend/Dockerfile`
  - Updated to Python 3.12
  - Added `FLASK_ENV=production`
  - Removed `network_mode: host` from docker-compose

- **File:** `docker-compose.yml`
  - Local development only (PostgreSQL container)

- **File:** `docker-compose.prod.yml`
  - Production configuration without PostgreSQL
  - Uses external PostgreSQL database
  - Removed obsolete `version: '3.8'` attribute

### 2. Database Migrations Fix
- **File:** `backend/alembic/env.py`
  - Added `DATABASE_URL` environment variable support
  - Offline and online modes now read from env var
  - Fixed `KeyError: 'url'` error in container

### 3. Frontend Production Build
- **File:** `frontend/vite.config.ts`
  - Removed `base` from config (moved to CI build command)
  - Local dev works without base URL

- **File:** `frontend/src/vite-env.d.ts`
  - Added TypeScript types for `import.meta.env`
  - Fixed `Property 'env' does not exist on type 'ImportMeta'`

- **File:** `frontend/src/api.ts`
  - Changed from `/api` to `${import.meta.env.VITE_API_URL}/api`
  - Added debug console logs for API URL

- **File:** `frontend/src/contexts/AppContext.tsx`
  - Socket.IO now uses `import.meta.env.VITE_API_URL`
  - Changed URL updates from `window.history.pushState` to `window.location.hash`
  - HashRouter compatibility fixes

### 4. GitHub Pages Deployment
- **File:** `.github/workflows/deploy-frontend.yml`
  - Created GitHub Action for automated deployment
  - Triggers on push to `main` branch
  - Passes `VITE_API_URL` secret during build
  - Uses `actions/deploy-pages@v4`
  - Build command includes `--base=/scrum-poker-llm/`

### 5. Router Change
- **File:** `frontend/src/App.tsx`
  - Changed from `BrowserRouter` to `HashRouter`
  - Required for GitHub Pages static hosting
  - URLs changed from `/room/:id` to `/#/room/:id`

### 6. TypeScript Fixes
- **File:** `frontend/src/App.tsx`
  - Removed unused `useApp` import

- **File:** `frontend/src/pages/HomePage.tsx`
  - Removed unused `useNavigate` import
  - Removed unused `room` destructuring
  - Added back `useSearchParams` hook

- **File:** `frontend/src/pages/RoomPage.tsx`
  - Added null checks for `room` and `currentPlayer`
  - Created `roomData` and `currentPlayerData` aliases after guards
  - Fixed `handleLeaveRoom` to check null before accessing properties

### 7. Environment Configuration
- **File:** `.env.example`
  - Updated with production-ready defaults
  - Added `VITE_API_URL` for frontend

- **File:** `frontend/package.json`
  - Added `homepage` field for GitHub Pages

### 8. Footer Addition
- **File:** `frontend/src/App.tsx`
  - Added `<footer>` with GitHub repository link
  - Wrapped content in `app-wrapper` and `app-main` divs

- **File:** `frontend/src/App.css`
  - Added `.app-wrapper` flex layout (min-height: 100vh)
  - Added `.app-footer` styles (centered, border-top)
  - Added `.footer-link` hover effects

## Files Created

- `.github/workflows/deploy-frontend.yml`
- `docker-compose.prod.yml`
- `frontend/src/vite-env.d.ts`
- `DEPLOY.md`
- `scripts/deploy.sh`
- `backend/.dockerignore`

## Files Modified

- `backend/Dockerfile`
- `backend/alembic/env.py`
- `docker-compose.yml`
- `frontend/vite.config.ts`
- `frontend/src/App.tsx`
- `frontend/src/App.css`
- `frontend/src/api.ts`
- `frontend/src/contexts/AppContext.tsx`
- `frontend/src/pages/HomePage.tsx`
- `frontend/src/pages/RoomPage.tsx`
- `frontend/package.json`
- `.env.example`
- `README.md`

## Deployment URLs

- **Frontend:** https://safonovpro.github.io/scrum-poker-llm
- **Backend:** http://your-server-ip:5000 (Docker container on Ubuntu 22.04)
- **Database:** External PostgreSQL on same server

## Key Decisions

- HashRouter over BrowserRouter for static hosting compatibility
- External PostgreSQL in production (not containerized)
- API URL via GitHub Secret (not hardcoded)
- `window.location.hash` over `history.pushState` for HashRouter
- Separate docker-compose files for local vs production

## Issues Resolved

1. `Property 'env' does not exist on type 'ImportMeta'` — added vite-env.d.ts
2. `KeyError: 'url'` in Alembic — read DATABASE_URL from env
3. API requests going to wrong URL — VITE_API_URL secret in GitHub Actions
4. 404 on GitHub Pages after navigation — switched to HashRouter
5. URL missing base path — used `window.location.hash` instead of `history.pushState`
6. Unused imports causing TS errors — cleaned up imports
